{
  "name": "agent-registry-worker",
  "version": "1.0.0",
  "description": "BlackRoad Agent Registry — Cloudflare Worker API",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node test/worker.test.js",
    "tail": "wrangler tail"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "3.99.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
